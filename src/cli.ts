#!/usr/bin/env node
// Pre-0.1.0 placeholder. The ported validators land here behind the parity
// oracle in parity/ — see parity/README.md.
console.error("workspace-kit: not implemented yet (pre-0.1.0)");
process.exit(2);
